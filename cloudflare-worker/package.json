{
  "name": "outfit-oracle-proxy",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest --config vitest.config.js"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "@neondatabase/serverless": "^0.10.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
